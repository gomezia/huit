{#
/**
 * @file
 * Theme override to display a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region div.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @see template_preprocess_region()
 */
#}
{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    {{ content }}
  </div>
{% endif %}

<ul class="social-networks">
	<li><a href="#" title="Suivez la Métropole de Lyon sur Twitter (nouvelle fenêtre)" target="_blank" class="twitter"><i class="fa fa-twitter"></i></a></li>
	<li><a href="#" title="Suivez la Métropole de Lyon sur Facebook (nouvelle fenêtre)" target="_blank" class="facebook"><i class="fa fa-facebook"></i></a></li>
	<li><a href="#" title="Suivez la Métropole de Lyon sur Instagram (nouvelle fenêtre)" target="_blank" class="instagram"><i class="fa fa-instagram"></i></a></li>

</ul>

